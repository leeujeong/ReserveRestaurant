<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.reserve.restaurant.repository.QnaRepository">
	
	<select id="selectQnaList1" resultType="Qna">
		SELECT *
		  FROM QNA
		 WHERE STATE = 1
		 ORDER BY QNA_NO DESC
	</select>
	
	<select id="selectQnaList2" resultType="Qna">
		SELECT *
		  FROM QNA
		 WHERE STATE = 2
		 ORDER BY QNA_NO DESC
	</select>
	
	<select id="selectQnaByNo" parameterType="Long" resultType="Qna">
		SELECT *
		  FROM QNA
		 WHERE QNA_NO = #{qnaNo}
	</select>
	
	<update id="updateQnaHit" parameterType="Long">
		UPDATE QNA
		   SET QNA_HIT = QNA_HIT + 1
		 WHERE QNA_NO = #{qnaNo}
	</update>

	<insert id="insertQna" parameterType="Qna">
		INSERT INTO QNA
		VALUES (QNA_NO_SEQ.NEXTVAL, #{qnaWriter}, #{qnaTitle}, #{qnaContent}, QNA_HIT, SYSDATE)
	</insert>
	
	<update id="updateQna" parameterType="Qna">
		UPDATE QNA
		   SET QNA_TITLE = #{qnaTitle}, QNA_CONTENT = #{qnaContent}
		 WHERE QNA_NO = #{qNo}
	</update>
	
	<delete id="deleteQna" parameterType="Long">
		DELETE
		  FROM QNA
		 WHERE QNA_NO = #{qnaNo}
	</delete>
	
</mapper>